# 本轮开发任务包（CR-005 + CR-007 + CR-008）

> **策划**：三件一起做；建议实现顺序如下。  
> **安装包**：仍搁置。  
> **状态：已验收收口（2026-07-17）** — 策划单屏可验项全部通过；双屏跟屏以开发自测为准，待有双屏再抽测。  
> **绿色包**：`publish/ScreenAnnotator/ScreenAnnotator.exe`  

---

## 建议顺序

1. **CR-007 清屏二次确认**（改动小，先合）→ `docs/CR-007-清屏二次确认.md`  
2. **CR-005 多显示器选屏** → `docs/CR-005-多显示器选屏.md`  
3. **CR-008 独立空白白板** → `docs/CR-008-独立空白白板.md`（依赖选中屏概念，故放多屏之后或并行注意跟屏）

---

## 发给开发的话术（可直接复制）

---

请按本轮任务包实现以下三单（安装包不做）：

1. **CR-007** `docs/CR-007-清屏二次确认.md`  
   有笔迹时清屏（按钮+快捷键）须二次确认；确认后仍可撤销。

2. **CR-005** `docs/CR-005-多显示器选屏.md`  
   工具栏可选屏幕；标注窗+顶栏跟屏；截屏打当前选中屏。策划只有单屏，多屏请你自测；单屏入口可用不崩即可。

3. **CR-008** `docs/CR-008-独立空白白板.md`  
   增加「空白白板」模式 B（不透明白底）；与屏幕标注 A 切换且 **两套笔迹互不影响**；B 下不能点穿桌面；保存文件兼容旧版（旧文件当仅 A）。清屏只清当前模式并走 CR-007。

建议顺序：007 → 005 → 008。全部按各文档验收项自测后更新绿色包并回复完成项。

---

**文档结束。**
